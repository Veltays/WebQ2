import initFormController from "./formController.js";

/**
 * Initialise le contrôleur du formulaire une fois que le DOM est prêt.
 */
document.addEventListener("DOMContentLoaded", () => {
    initFormController();
});
