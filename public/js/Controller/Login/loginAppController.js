import initLoginPage from "./loginController.js";

/**
 * Initialise la page de connexion une fois que le DOM est complètement chargé.
 */
document.addEventListener("DOMContentLoaded", () => {
    initLoginPage();
});
